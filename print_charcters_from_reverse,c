#include <stdio.h>

int main(void) {
    char array[1000],len,i;
    scanf("%s",array);
    len=strlen(array);
    for(i=len-1;i>0;i--)
    {
    	if(array[i]!=array[i-1])
    	printf("%c",array[i]);
    }
	return 0;
}
